generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Employee {
  id        String   @id @default(uuid())
  name      String
  shifts    Json
  hours     Json
  createdAt DateTime @default(now())
}